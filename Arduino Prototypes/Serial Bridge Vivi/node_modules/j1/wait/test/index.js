const assert = require('assert');
const wait = require('..');

describe('wait', function () {

	it('should wait for a while', function () {
		const started = Date.now();
		return wait(100).then(() => {
			assert.equal(50 < Date.now() - started, true);
		});
	});

});
