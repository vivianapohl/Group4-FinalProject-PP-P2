function forEachKey(object, fn, thisArg = object) {
	for (const key of Object.keys(object)) {
		if (fn.call(thisArg, object[key], key, object)) {
			break;
		}
	}
	return object;
}

module.exports = forEachKey;
